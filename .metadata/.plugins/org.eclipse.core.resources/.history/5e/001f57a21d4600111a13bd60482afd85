object Problem52 {

  def main(args : Array[String]) : Unit = {
    
    //each spot in array represents whether number has that digit
    val hasDigitArray = Array.fill(10) {false}
    
    for(i <- 1 to 100;
        j <- 2 to 6) {
       
      
      
    }
    
  }
  
  
  //both array and hashmap have constant time lookup, right?
}