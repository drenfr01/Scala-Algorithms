object foldAndReduce {

  def main(args: Array[String]): Unit = {
    println(sum(List(2..10)))
  }

  def sum (xs : List[Int]) = xs reduceLeft((x,y) => x + y)
  
  def produe(xs : List[Int]) = xs reduceLeft ((x,y) => x * y)
}